import Button from "./button";

const Header = () => {
  return (
    <header className="header">
      <h1>Task Tracker</h1>
      <Button color="green" />
    </header>
  );
};

export default Header;
